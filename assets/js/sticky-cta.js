/**
 * sticky-cta.js — NutriNest Sticky Mobile CTA Bar (Disabled)
 *
 * Disabled in favor of the fixed 5-item mobile bottom navigation tab bar.
 */

export function initStickyCta() {
  // No-op: bottom tab bar is permanently fixed on mobile
}
